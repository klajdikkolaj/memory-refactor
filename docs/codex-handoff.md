# Codex Handoff

- Goal: Build the initial AI memory refactor product structure.
- Current state: Standard Codex operating scaffold plus initial product monorepo structure is in place. Raw memory events, canonical memory units, preview refactor plans, Temporal workflow-start runs, review/apply state, pgvector memory embeddings, and temporal relationship edges are now persisted through Postgres-backed repositories.
- Files changed: Added repo docs, planning docs, testing strategy, repo-local Codex skills and reviewer agents, Next.js web shell, Python memory-engine service, database foundation, raw memory event API/persistence, memory create/list API persistence, refactor run/operation preview persistence, Temporal workflow start endpoint, Temporal activity persistence path, manual raw-memory batch flow, review/apply UI wiring, pgvector embedding contracts/schema/repository, temporal memory graph contracts/schema/repository, local infra Compose files, and FE/BE test harnesses.
- Checks run: `make check-structure`, `docker compose config`, `make test-python-unit`, `make test-python-integration`, `make check-web`, `make test-web-unit`, `make test-web-e2e`, `uv run --extra dev ruff check src tests alembic`, `uv run --extra dev pytest`, Alembic `upgrade head` and `downgrade base` against a clean throwaway database, JSON parse checks for package manifests, TOML parse check for `services/memory-engine/pyproject.toml`, ASCII scan for added scaffold files.
- Open risks: CI is still pending as `T9.4`. The default local `memory_refactor` database may contain tables created by earlier `Base.metadata.create_all` integration runs, so `make migrate` can fail there until that dev schema is reset or stamped; migrations validated successfully on a clean throwaway database.
- Next recommended step: Start `T4.1` from `docs/tasks.md`: add a Pydantic AI agent wrapper behind the `RefactorAgent` port while keeping the deterministic stub as fallback.
